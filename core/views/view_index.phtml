<?php
	global $userLogged;
?>
<main class="container" id="accueil">
	<div class="buttons">
		<?php if ($userLogged->hasRole(DAORoles::getById(3)) OR !$userLogged->isAuthentified()): ?>
			<a href="<?= getUrl('evaluations', 'student', 'view') ?>" class="btn btn-black-on-white">Voire mes évaluations</a>
			<a href="<?= getUrl('evaluations', 'files', 'send') ?>" class="btn btn-black-on-white">Envoyer un fichier</a>
		<?php endif; ?>

		<?php if ($userLogged->isAuthentified() AND ($userLogged->isAdmin() OR $userLogged->hasRole(DAORoles::getById(1)) OR $userLogged->hasRole(DAORoles::getById(3)))): ?>
			<a href="<?= getUrl('evaluations', 'admin', 'view') ?>" class="btn btn-black-on-white">Voir les évaluations</a>
			<a href="<?= getUrl('evaluations', 'files', 'view') ?>" class="btn btn-black-on-white">Voir les envois</a>
		<?php endif; ?>
	</div>
</main>